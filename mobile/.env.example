# Backend API base URL (must include the /api/v1 prefix)
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1

# Agora App ID (safe to expose client-side; the App Certificate stays server-only)
EXPO_PUBLIC_AGORA_APP_ID=
